# 実装済み機能ログ

最終更新: 2026-04-02

---

## Phase A（2026-04-02 実装完了）

### A-2: /lp/toeic SEO LP（mon-landing）

**実装内容**
- `lp/toeic.html` を新規作成
- `vercel.json` に `/lp/toeic` → `/lp/toeic.html` の rewrite を追加

**機能概要**
- ターゲット: 「TOEIC 単語アプリ」「Quizlet 代わり」「英語学習 AI」で検索するユーザー
- 構成: ヒーロー / Quizlet比較テーブル / 3ステップ / 価格ラダー / FAQ / フッターCTA
- SEO: `<title>`, `<meta description>`, JSON-LD（SoftwareApplication・FAQPage）, canonical, OGP設定済み

**変更ファイル**
- `/Users/ryuki/Desktop/mon-landing/lp/toeic.html`（新規）
- `/Users/ryuki/Desktop/mon-landing/vercel.json`（rewrite追加）

---

### A-1: 偏差値カード X 共有 OGP（EIGOモン）

**実装内容**
- `/api/og/score` エンドポイント新規作成（Edge Runtime）
- result/page.tsx の `handleShare` に偏差値スコアを含めるよう更新

**機能概要**
- `/api/og/score?deviation=62&score=3&max=4&mode=TOEIC` でOGP画像（1200×630）を生成
- 偏差値の数値を大きく（140px）表示し、スコアに応じて色が変わる（金・緑・青・赤）
- X（Twitter）共有テキストが「EIGOモンで偏差値62を記録！」に変更

**変更ファイル**
- `/Users/ryuki/Desktop/eigomon/src/app/api/og/score/route.tsx`（新規）
- `/Users/ryuki/Desktop/eigomon/src/app/result/page.tsx`（handleShare更新）

---

### A-3: 紹介バナー + URLパラメータ保存（EIGOモン・SAKUモン）

**実装内容**
- `auth/page.tsx` に `?ref=XXXX` URLパラメータ → localStorage 保存処理を追加（Suspense対応）
- `home/page.tsx` に紹介URLコピーバナーを追加

**機能概要**
- 紹介リンク（`/auth?ref=紹介コード`）を踏むと localStorage に保存 → 登録時に自動で紹介報酬付与
- ホーム画面に「友達に紹介するとスキャン3回プレゼント」バナーを表示（コピーボタン付き）
- EIGOモン: `eigomon_ref` キー / SAKUモン: `sakumon_ref` キー

**変更ファイル**
- `/Users/ryuki/Desktop/eigomon/src/app/auth/page.tsx`（useSearchParams + Suspense追加）
- `/Users/ryuki/Desktop/sakumon/src/app/auth/page.tsx`（同上）
- `/Users/ryuki/Desktop/eigomon/src/app/home/page.tsx`（紹介バナー追加）
- `/Users/ryuki/Desktop/sakumon/src/app/home/page.tsx`（紹介バナー追加）

---

### A-4: ¥398→MONパス アップセル通知（EIGOモン・SAKUモン）

**実装内容**
- `/api/cron/light-pass-upsell` エンドポイント新規作成
- `vercel.json` に毎朝9時の cron 追加
- `.env.local` に `CRON_SECRET` / `RESEND_API_KEY` 追加
- `billing/page.tsx` に残り3日以下のアップセルバナー追加

**機能概要**
- 毎朝9時に Vercel Cron が `/api/cron/light-pass-upsell` を呼び出す
- `light_pass_end` が昨日切れた・かつ `subscription_status` が非アクティブなユーザーを検索
- Resend でアップセルメール送信（「MONパスに切り替える →」ボタン付き）
- 送信後 `light_pass_status` を `expired` に更新
- billing ページにも残り3日以下で帯バナーを表示

**環境変数（Vercelにも設定が必要）**
- `CRON_SECRET`: `18b34401ecf3b1e15b5a56ba4a2609c1ea7c7c076e667f45581a931006b48375`
- `RESEND_API_KEY`: `re_4PMPJ77Q_KDZanZZ9cvNtcdkULEewv5UZ`（SAKUモンは新規追加）

**変更ファイル**
- `/Users/ryuki/Desktop/eigomon/src/app/api/cron/light-pass-upsell/route.ts`（新規）
- `/Users/ryuki/Desktop/sakumon/src/app/api/cron/light-pass-upsell/route.ts`（新規）
- `/Users/ryuki/Desktop/eigomon/vercel.json`（cron追加）
- `/Users/ryuki/Desktop/sakumon/vercel.json`（cron追加）
- `/Users/ryuki/Desktop/eigomon/.env.local`（CRON_SECRET追加）
- `/Users/ryuki/Desktop/sakumon/.env.local`（RESEND_API_KEY・CRON_SECRET追加）
- `/Users/ryuki/Desktop/eigomon/src/app/billing/page.tsx`（アップセルバナー追加）
- `/Users/ryuki/Desktop/sakumon/src/app/billing/page.tsx`（アップセルバナー追加）

---

## デプロイ前に必要な手動作業

| 作業 | 対象 | 状態 |
|---|---|---|
| Vercel環境変数 `CRON_SECRET` 設定 | EIGOモン・SAKUモン | ⬜ 未完了 |
| Vercel環境変数 `RESEND_API_KEY` 設定 | SAKUモン | ⬜ 未完了 |
| git push & Vercel deploy | EIGOモン・SAKUモン・mon-landing | ⬜ 未完了 |

---

## 既実装済み（Phase A開始前）

| 機能 | EIGOモン | SAKUモン |
|---|---|---|
| ¥398 エントリーパス Stripe決済 | ✅ | ✅ |
| 紹介コード生成 `/api/referral` | ✅ | ✅ |
| 紹介記録・報酬付与 `/api/track-ref` | ✅ | ✅ |
| light_pass_status / light_pass_end DB カラム | ✅ | ✅ |
| 週次レビュー Cron `/api/weekly-review` | ✅ | — |
| 月次レポート Cron `/api/report` | ✅ | ✅ |
| 偏差値計算 `/api/deviation` | ✅ | — |
| 結果シェア `/api/share` | ✅ | — |
